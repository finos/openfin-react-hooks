enum WINDOW_STATE {
  INITIAL = "INITIAL",
  LAUNCHING = "LAUNCHING",
  LAUNCHED = "LAUNCHED",
  POPULATING = "POPULATING",
  POPULATED = "POPULATED",
  ERROR = "ERROR",
}

export default WINDOW_STATE;
